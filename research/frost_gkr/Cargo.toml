[package]
name = "frost-gkr-bench"
version = "0.1.0"
edition = "2021"
publish = false

# Keep the paper artifact out of the production workspace and its default
# build/test graph. The runner copies this crate into the pinned worktree.
[workspace]

[dependencies]
elide_core = { path = "../../elide_core" }
elide_gkr = { path = "../../elide_gkr" }
elide_poseidon2b = { path = "../../elide_poseidon2b" }

/target/
**/target/
# Build directories from parallel verification runs. A release refuses to
# publish from a tree that is not clean, and these grow to tens of gigabytes,
# so leaving them untracked would block it.
/target-*/
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
__pycache__/
*.py[cod]

# Golden vectors are 8.7 MB and reproducible in one command; see miner/README.md
miner/golden.txt

# GPU miner sources are distributed privately, never in this repository.
/miner/
